<% @title = '商品の新規登録' %>
<h1><%= @title %></h1>

<div id="generic-form">
  <%= form_for [ :admin, @product ] do |f| %>
    <%= render 'form', f: f %>
    <div class="buttons">
      <%= f.submit '登録' %>
      <%= link_to 'キャンセル', :admin_products %>
    </div>
  <% end %>
</div>
